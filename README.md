# Lab Test

A React Native app built with Expo, inspired by Facebook UI.

## Installation

1. Install dependencies: `npm install`

2. Start the app: `npm start`

## Usage

Run on iOS/Android/Web using Expo CLI.

## Troubleshooting

If icons don't load, ensure @expo/vector-icons is installed.

If navigation issues, check React Navigation setup.
